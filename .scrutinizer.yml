inherit: true

checks:
  php:
    code_rating: true
    duplication: true

filter:
  paths: [code/*, tests/*]

build:
  tests:
    override:
      -
        command: 'phpunit --coverage-clover=some-file'
        coverage:
          file: 'some-file'
          format: 'php-clover'
